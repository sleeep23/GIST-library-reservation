# 사용법

## 실행

```sh
npm install
npm run dev
```

`http://localhost:5173`에 접속합니다. 서버를 재시작하면 로그인 상태가 사라지므로 다시 로그인해야 합니다.

## 로그인과 토큰

- 앱을 켤 때 GIST 도서관 아이디와 비밀번호로 로그인합니다.
- 비밀번호는 저장하지 않습니다.
- 발급된 access token은 서버 프로세스 메모리에만 보관합니다.
- 날짜를 바꿔 조회할 때마다 다시 로그인하지 않고, 서버 메모리의 토큰으로 요청합니다.
- 로그아웃하거나 서버를 종료하면 토큰과 조회 캐시가 사라집니다.

## 예약 현황

1. 상단의 `예약 현황` 탭을 선택합니다.
2. 날짜를 선택합니다.
3. 필요하면 층, 호실 그룹, 상태 필터를 조정합니다.
4. 표에서 시간대별 상태를 확인합니다.
5. 예약 가능한 칸을 누르면 예약 확인 창이 뜹니다.
6. 내 예약 칸을 누르면 취소 확인 창이 뜹니다.

조회 중에는 기존 표 위에 로딩 상태가 표시됩니다. 새 날짜의 데이터가 도착하기 전까지 이전 날짜와 섞여 보이지 않도록 로딩 상태를 따로 표시합니다.

## 내 예약

1. 상단의 `내 예약` 탭을 선택합니다.
2. `목록` 보기에서 날짜, 시간, 호실을 한 줄씩 확인합니다.
3. `달력` 보기에서 예약이 있는 날짜만 모아 확인합니다.
4. 예약 항목을 누르면 취소 확인 창이 뜹니다.

내 예약 조회는 도서관의 예약 가능 기간 전체를 기준으로 가져옵니다.

## 문제 해결

- `로그인이 필요합니다`: 서버가 재시작됐거나 로그아웃된 상태입니다. 다시 로그인하세요.
- `도서관 API 응답 시간이 초과되었습니다`: 도서관 서버 응답이 지연된 상태입니다. 잠시 후 다시 조회하세요.
- 날짜를 바꿨는데 예전 표처럼 보임: 로딩 표시가 끝나고 상단 조회 날짜와 표 날짜가 같은지 확인하세요.
- Vercel에서 API가 503으로 막힘: [Vercel 배포 가이드](./docs/VERCEL_DEPLOYMENT.md)의 메모리 세션 차단 정책을 확인하세요.

## 배포 전 확인

```sh
npm run typecheck
npm run test
npm run build
npm audit --audit-level=moderate
```

GitHub에 올리기 전 `git status --short`와 검색으로 `.env`, `.vercel`, HAR 파일, 네트워크 캡처 이미지, access token, refresh token, 비밀번호가 포함되지 않았는지 확인하세요.
